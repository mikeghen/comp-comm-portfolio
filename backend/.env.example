# Used for CORS
ENVIRONMENT=development

# AgentKit Environment Variables
NETWORK_ID=11155111

# Agent/AgentKit private key (must have AGENT_ROLE on MessageManager contract)
# This account will call markMessageProcessed() and needs ETH for gas fees
PRIVATE_KEY=0x...

# Langgraph Environment Variables
OPENAI_API_KEY="sk-proj-..."

# Blockchain Event Listener Configuration
# WebSocket RPC URL for Ethereum Sepolia (required for real-time event listening)
# Get this from Infura, Alchemy, or other providers
ETHEREUM_RPC_URL=wss://sepolia.infura.io/ws/v3/YOUR_PROJECT_ID

# MessageManager contract address on Sepolia (default provided, but can be overridden)
MESSAGE_MANAGER_CONTRACT_ADDRESS=0xDa779e0Ed56140Bd700e3B891AD6e107E0Ef764D

# Event polling interval in seconds (optional, default: 2.0)
EVENT_POLL_INTERVAL=2.0

# Optional: LangSmith for tracing (uncomment to enable)
# LANGSMITH_API_KEY=your_langsmith_api_key_here
# LANGSMITH_TRACING=true 